package dev.desktop;
import javax.swing.*;

public class core extends JFrame{
    JFrame frame;
    // interface to write extensions for Lens
    public core(String windowName) {
        this.frame = new JFrame(windowName);
    }
    // todo
}
